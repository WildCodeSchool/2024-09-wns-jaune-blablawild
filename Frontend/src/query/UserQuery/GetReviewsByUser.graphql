query GetReviewsByUser($userId: String!) {
  getReviewsByUser(userId: $userId) {
    date
    comment
    id
    notation
    sender {
      profile {
        image
      }
      firstname
    }
  }
}