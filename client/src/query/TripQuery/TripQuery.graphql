query GetTrip($data: FilterTripInput!) {
  getTrip(data: $data) {
    id
    departure_time
    departure_city
    capacity
    arrival_city
    driver {
      id
      firstname
      lastname
      profile {
        image
      }
    }
    price
    bookings {
      id
      seatsCount
      passenger {
        id
        firstname
        lastname
      }
    }
  }
}